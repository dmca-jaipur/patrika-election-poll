/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['your-project-id.supabase.co'], // यहाँ अपनी सुपाबेस URL का डोमेन डाल सकते हैं
    },
};

export default nextConfig;
